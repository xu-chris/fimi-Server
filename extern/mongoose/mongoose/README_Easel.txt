This is a version of mongoose pre-built for Easel for Linux, Windows and Mac (TODO).

To use mongoose, all you need to do is include the right library directories and the header mongoose.h. There is a class in common that wraps all this together.
